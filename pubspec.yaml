name: zeta
description: Zeta
version: 0.0.1
homepage:

environment:
  sdk: ">=2.19.1 <3.0.0"
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.zebra.zeta.zeta
        pluginClass: ZetaPlugin
      ios:
        pluginClass: ZetaPlugin
