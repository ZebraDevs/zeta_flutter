name: zeta_flutter
description: Zeta
version: 0.0.1+6
homepage:

environment:
  sdk: ">=2.19.1 <3.0.0"
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  dart_code_metrics: ^5.6.0
  dart_code_metrics_presets: ^1.4.1
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  widgetbook: ^2.4.1

flutter:
