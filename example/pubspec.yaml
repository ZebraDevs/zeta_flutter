name: zeta_example
description: Demonstrates how to use zeta
version: 0.0.1
publish_to: "none"

environment:
  sdk: ">=2.19.1 <3.0.0"

dependencies:
  collection: ^1.17.0
  flutter:
    sdk: flutter
  go_router: ^6.5.5
  google_fonts: ^4.0.3
  widgetbook: ^3.0.0-beta.14
  widgetbook_models: ^3.0.0-beta.4
  zeta_flutter:
    path: ../

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
